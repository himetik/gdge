services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: on-failure:1
